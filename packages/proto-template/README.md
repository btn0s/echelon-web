# @echelon/proto-template

Base template for new Echelon prototypes.

Use via:

```bash
pnpm create-proto proto-XX-system
```

The script copies `packages/proto-template/template` into `apps/<name>` and replaces template placeholders.
